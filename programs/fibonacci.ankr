function fibonacci(n) {
  if (n <= 0) {
    return 0;
  } else if (n == 1) {
    return 1;
  }

  var a = 0, b = 1, sum = 0;
  for (var i = 2; i <= n; i++) {
    sum = a + b;
    a = b;
    b = sum;
  }
  return b;
}

for (var i = 0; i < 10; i++) {
  output(fibonacci(i));
}
